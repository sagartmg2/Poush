

const checkToken = () => {
    console.log("check token")
}

checkToken()

/* 
default export 
    - only one per moudule/ .js file
 */
module.exports = checkToken  // direct funciton is being export unlike in named export where an object is exported